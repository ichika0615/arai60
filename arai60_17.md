# Step 1 
グラフを用いると考えて図などを考えてみたが、よくわからなかった。

# Step 2
- 他の人のコードを見る。丸写しは意味ないので、大体読んだら自分で書くようにした。
## 陸地(```'1'```)を検出したら、カウントを1増やして、再帰関数を用いて繋がってる陸地を海で塗りつぶす。全部海になったら終わり。
### DFS
```python
class Solution:
    def numIslands(self, grid: List[List[str]]) -> int:
        def mark_island_as_visietd(i, j):
            if not (0 <= i < column_length and 0 <= j < row_length):
                return 
            if grid[i][j] == '0':
                return
            grid[i][j] = '0'
            mark_island_as_visietd(i-1, j)
            mark_island_as_visietd(i+1, j)
            mark_island_as_visietd(i, j-1)
            mark_island_as_visietd(i, j+1)
        
        number_of_islands = 0
        column_length = len(grid)
        row_length = len(grid[0])
        for i in range(column_length):
            for j in range(row_length):
                if grid[i][j] == '1':
                    number_of_islands += 1
                    mark_island_as_visietd(i, j)
        
        return　number_of_islands         
```
- 入力に対して破壊的で、場合にもよるが好ましくない。
- ```row_length = len(grid[0])```の0はマジックナンバーか。
- 再帰呼び出しのオーバーヘッドは、最大で(全部陸地)
- 時間計算量: $O(M*N)$ (M、Nをそれぞれ縦横の長さとする)
- 空間計算量: $O(1)$
- ChatGPTと話していたら、DFSだと言われて驚いた。DFSはグラフとセットで使うと思っていたが、「行けるところまで行く」戦略を定式化したものなのかもしれない。

```python
grid_copy = [[0]*row_length for _ in range(column_length)]
for i in range(column_length):
  for j in range(row_length):
    grid_copy = grid[i][j]
```
```python
import copy
grid_copy = copy.deepcopy(grid)
```
とでもすれば空間計算量$O(MN)$を要するが入力に対して非破壊的にすることができる。

## BFS
```python
class Solution:
    def numIslands(self, grid: List[List[str]]) -> int:
        def remove_islands(colum, row):
            lands = deque([(colum, row)])
            while lands:
                column, row = lands.popleft()
                if not (0 <= column < column_length and 0 <= row < row_length):
                    continue 
                if grid[column][row] == '0':
                    continue
                grid[column][row] = '0'
                directions = [(0, 1), (0, -1), (1, 0), (-1, 0)]
                for di_c, di_r in directions:
                    lands.append((column + di_c, row + di_r))
        
        column_length = len(grid)
        row_length = len(grid[0])
        number_of_islands = 0
        for i in range(column_length):
            for j in range(row_length):
                if grid[i][j] == '1':
                    number_of_islands += 1
                    remove_islands(i, j)
        
        return number_of_islands
```
- 時間計算量$O(M*N)$  空間計算量$O(1)$
- 正直DFSとBFSの違いがよくわからない。どちらも「行けるところまで行く」戦略に従っているように見える。

## Union Find

- ユニオンファインドを用いる。二次元のマップを一次元化してlands.parentsにて管理することになる。陸地を検出したらunionしてまとめて、結局繋がってる陸地はグルーピングされるから島の数がわかる。

### ↓これは動かない。(33/49 testcases passed)
```python
class UnionFind:
    def __init__(self, size):
        self.parents = [i for i in range(size)]

    def find(self, node):
        root = node
        while self.parents[root] != root:
            root = self.parents[root]
        return root
    
    def union(self, node_1, node_2):
        root_1 = self.find(node_1)
        root_2 = self.find(node_2)
        if root_1 != root_2:
            self.parents[root_2] = root_1
    
class Solution:
    def numIslands(self, grid: List[List[str]]) -> int:
        height = len(grid)
        width = len(grid[0])
        lands = UnionFind(height*width)

        def get_index_in_unionfind(row, column):
            return row * width + column
        
        directions = [(0, 1), (1, 0), (0, -1), (-1, 0)]
        for h in range(height):
            for w in range(width):
                if grid[h][w] == '1':
                    for dir_r, dir_c in directions:
                        if 0 <= h + dir_r < height and 0 <= w + dir_c < width and grid[h + dir_r][w + dir_c] == '1':
                            lands.union(get_index_in_unionfind(h, w), get_index_in_unionfind(h + dir_r, w + dir_c))
        
        types_of_islands = set()
        for h in range(height):
            for w in range(width):
                if grid[h][w] == '1':
                    types_of_islands.add(lands.parents[get_index_in_unionfind(h, w)])
        
        return len(types_of_islands)

```
```python
[['1', '1', '1'],
 ['0', '1', '0'],
 ['1', '1', '1']]
```
のtestcaseでうごかなかった。`lands.parents = [0, 0, 0, 3, 6, 5, 6, 6, 6]`
となり、繋がっている陸なのにrootが0と6の２種類になってるっぽい。

# Step 3

## DFS
- LANDとWATERを用いる。VISITED = 2 を宣言している人もいた。
```python 
LAND = '1'
WATER = '0'

class Solution:
    def numIslands(self, grid: List[List[str]]) -> int:
        def remove_one_island(row, column):
            if not (0 <= row < row_length and 0 <= column < column_length):
                return None
            if grid[row][column] == WATER:
                return None
            grid[row][column] = WATER
            remove_one_island(row+1, column)
            remove_one_island(row-1, column)
            remove_one_island(row, column+1)
            remove_one_island(row, column-1)
        
        row_length = len(grid)
        column_length = len(grid[0])
        islands_counter = 0

        for i in range(row_length):
            for j in range(column_length):
                if grid[i][j] == LAND:
                    remove_one_island(i, j)
                    islands_counter += 1
        
        return islands_counter 

```
- LANDとかWATERとか全部大文字なんて珍しいなと思ったら、定数は大文字らしい。
- https://pep8-ja.readthedocs.io/ja/latest/#id23

『定数は通常モジュールレベルで定義します。全ての定数は大文字で書き、単語をアンダースコアで区切ります。例として MAX_OVERFLOW や TOTAL があります。』

- スタックオーバーフローの危険性について考える。
- コールスタックはベースケースとなる関数がpushされたらすぐにpopする。つまり、見た目は再帰関数が連続しているがいうほど深さがない。具体的には、どこかの一列(行)がすべてLANDの時にコールスタックの深さが最大となるから、せいぜいmax(row_length, column_length)。
- 制約条件によるとmax(row_length, column_length)=300よりスタックオーバーフローは起こらない。

## 非再帰DFS
- 入力に対して非破壊的
- 普通に難しく20分近くかけてERRORを繰り返しつつどうにかAC。
```python
LAND = '1'
WATER = '0'

class Solution:
    def numIslands(self, grid: List[List[str]]) -> int:
        def visit_island(row, column):
            if not (0 <= row < row_length and 0 <= column < column_length and 
            grid[row][column] == LAND and (row, column) not in visited_areas):
                return None
            visited_areas.add((row, column))
            island = [(row, column)]

            while island:
                present_row, present_column = island.pop()
                visited_areas.add((present_row, present_column))
                for d_row, d_column in [(0, 1), (0, -1), (1, 0), (-1, 0)]:
                    new_row = present_row + d_row
                    new_column = present_column + d_column
                    if 0 <= new_row < row_length and 0 <= new_column < column_length and grid[new_row][new_column] == LAND and (new_row, new_column) not in visited_areas:
                        island.append((new_row, new_column))
        
        row_length = len(grid)
        column_length = len(grid[0])
        visited_areas = set()
        number_of_islands = 0

        for i in range(row_length):
            for j in range(column_length):
                if grid[i][j] == LAND and (i, j) not in visited_areas: 
                    visit_island(i, j)
                    number_of_islands += 1
                    
        return number_of_islands

```
- 『すべての行の長さを、最大79文字までに制限しましょう。』
- https://pep8-ja.readthedocs.io/ja/latest/#id8

## 再帰DFS(非破壊的)
- コピー作っただけ
```python
LAND = '1'
WATER = '0'

class Solution:
    def numIslands(self, grid: List[List[str]]) -> int:
        grid_copy = deepcopy(grid)
        def remove_island(row, column):
            if not (0 <= row < row_length and 0 <= column < column_length and grid_copy[row][column] == LAND):
                return None
            grid_copy[row][column] = WATER
            remove_island(row+1, column)
            remove_island(row-1, column)
            remove_island(row, column+1)
            remove_island(row, column-1)
        
        row_length = len(grid)
        column_length = len(grid[0])
        number_of_islands = 0

        for i in range(row_length):
            for j in range(column_length):
                if grid_copy[i][j] == LAND:
                    remove_island(i, j)
                    number_of_islands += 1

        return number_of_islands
```
- deepcopyについて調べた。
- https://docs.python.org/ja/3.13/library/copy.html#copy.deepcopy
- shallow copyとの違いを確認した。(ChatGPTに問い詰めてやっとわかった……)
- shallow copyでは外側だけ新しく作って、中身の要素は同じものを参照している。new_list = listとは違う。(全部同じ)
- deepcopyは中身も含めてすべて別のものを参照している。

