# Step1 
### 間違い↓
### 数字の組とその合計をハッシュマップに格納して、ソートして合計が小さい順k個を出力
- Wrong  Answer 
- [1, 1, 3, 4], [2, 3, 1, 6]などでは、(1, 1): 2 が二組出てくるが、ハッシュマップには１つしか書き込まれない。
```python
class Solution:
    def kSmallestPairs(self, nums1: List[int], nums2: List[int], k: int) -> List[List[int]]:
        pairs_to_sums = {}
        for num1 in nums1:
            for num2 in nums2:
                pairs_to_sums[(num1, num2)] = num1 + num2
        
        smallest_sums_pairs = []
        for (num1, num2), num_sum in pairs_to_sums.items():
            smallest_sums_pairs.append(((num1, num2), num_sum))
        
        smallest_sums_pairs.sort(key=itemgetter(1))
        
        if k < len(smallest_sums_pairs):
            smallest_sums_pairs = smallest_sums_pairs[:k]

        return [(num1, num2) for (num1, num2), _ in smallest_sums_pairs]
```
- よく考えたらハッシュマップに格納する必要性がない。
### 数字の組と合計をリストに直接タプルとして格納して、ソートして合計が小さい順k個を出力。
```python
class Solution:
    def kSmallestPairs(self, nums1: List[int], nums2: List[int], k: int) -> List[List[int]]:
        pair_totals = []  # pair_total is a list.
        
        for num1 in nums1:
            for num2 in nums2:
                pair_totals.append((num1 + num2, (num1, num2)))
        
        pair_totals.sort(key = lambda x: x[0])
        k_smalest_pairs = pair_totals[:k]

        return [(num1, num2) for _, (num1, num2) in k_smalest_pairs]
```
- 間違ってはなさそうだけど、MLE。
- 時間計算量: O(NMlogNM)　空間計算量: O(NM)
- 公式ドキュメントにはPythonリストの長さの最大値は載ってなかったが、64bit マシンだから2**64-1がとりえる整数の最大値で、リストにも大体これくらいの容量があるっぽい。
- https://stackoverflow.com/questions/855191/how-big-can-a-python-list-get
- Pythonのリストって動的に拡張されるから理論上は無尽蔵、つまり実際はハードウェアの容量に依存する。64bitマシンでもまちまちだと思う。
- 最大入力がnum1、num2ともに10^5なので、pair_totalsに10^5 * 10^5 = 10^10 入ることになる。なんか余裕で入る気がするけど。
- 計算時間は $10**10log(10**10) / 1000000$としたら13日ぐらいかかる。吐くならTLEだと思うが、なぜMLEだったのだろうか。

