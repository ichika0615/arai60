# Step1 
### 間違い↓
### 数字の組とその合計をハッシュマップに格納して、ソートして合計が小さい順k個を出力
- Wrong  Answer 
- [1, 1, 3, 4], [2, 3, 1, 6]などでは、(1, 1): 2 が二組出てくるが、ハッシュマップには１つしか書き込まれない。
```python
class Solution:
    def kSmallestPairs(self, nums1: List[int], nums2: List[int], k: int) -> List[List[int]]:
        pairs_to_sums = {}
        for num1 in nums1:
            for num2 in nums2:
                pairs_to_sums[(num1, num2)] = num1 + num2
        
        smallest_sums_pairs = []
        for (num1, num2), num_sum in pairs_to_sums.items():
            smallest_sums_pairs.append(((num1, num2), num_sum))
        
        smallest_sums_pairs.sort(key=itemgetter(1))
        
        if k < len(smallest_sums_pairs):
            smallest_sums_pairs = smallest_sums_pairs[:k]

        return [(num1, num2) for (num1, num2), _ in smallest_sums_pairs]
```
- よく考えたらハッシュマップに格納する必要性がない。
### 数字の組と合計をリストに直接タプルとして格納して、ソートして合計が小さい順k個を出力。
```python
class Solution:
    def kSmallestPairs(self, nums1: List[int], nums2: List[int], k: int) -> List[List[int]]:
        pairs_sums = []
        for num1 in nums1:
            for num2 in nums2:
                pairs_sums.append(((num1, num2), num1+num2))
        
        sorted_pairs_sums = sorted(pairs_sums, key=itemgetter(1))

        if k < len(sorted_pairs_sums):
            sorted_pairs_sums = sorted_pairs_sums[:k]
        
        return [list(pair) for pair, _ in sorted_pairs_sums]
```
- ……間違ってはなさそうだけど、MLE。
- 時間計算量: O(NM)　空間計算量: O(NM)

###　最大ヒープ(合計値が最大のものがルートに来るようなヒープ)を用いて、上位k個のデータを格納する。
- Pythonは最小ヒープがライブラリで提供されているが、合計値のマイナス倍を格納すれば実質的に最大ヒープとして使える。
```python
class Solution:
    def kSmallestPairs(self, nums1: List[int], nums2: List[int], k: int) -> List[List[int]]:
        pairs_sums = []  #heap
        for num1 in nums1:
            for num2 in nums2:
                heapq.heappush(pairs_sums, (-num1-num2, (num1, num2)))
                while k < len(pairs_sums):
                    heapq.heappop(pairs_sums)
        
        return [list(pair) for _, pair in pairs_sums]

```
- MLE
- 空間計算量はO(K)になったと思ったんだけど……
- 時間計算量: O(NMlogK) 空間計算量： O(K)

# Step 2


