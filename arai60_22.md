# Step 1
## BFS
```python
class Solution:
    def minDepth(self, root: Optional[TreeNode]) -> int:
        if not root:
            return 0
        
        nodes_to_visit = deque([(root, 1)])
        while nodes_to_visit:
            node, depth = nodes_to_visit.popleft()
            if not node.left and not node.right:
                return depth
            if node.left:
                nodes_to_visit.append((node.left, depth + 1))
            if node.right:
                nodes_to_visit.append((node.right, depth + 1))
        
```
- 一段ずつ見ていって、葉っぱを見つけ次第そこが一番浅い深さ。
- 時間計算量: O(N) (0.1sくらい)
- 空間計算量: O(N)
## DFS
```python
class Solution:
    def minDepth(self, root: Optional[TreeNode]) -> int:
        if not root:
            return 0
        
        nodes_to_visit = [(root, 1)] # This is a stack.
        MAX_DEPTH = 10**5
        min_depth = MAX_DEPTH
        while nodes_to_visit:
            node, depth = nodes_to_visit.pop()
            if not node.left and not node.right:
                min_depth = min(min_depth, depth)
            if node.left:
                nodes_to_visit.append((node.left, depth + 1))
            if node.right:
                nodes_to_visit.append((node.right, depth + 1))
        
        return min_depth
```
- 時間計算量: O(N)
- 空間計算量: O(N)
- 計算量の求め方がよくわからなかった。BTでは高さは最大Nだからこのようにしたが、最悪計算量であって、平均計算量がある気がする。
- DFSではガンガン最深部まで進んで、後から戻って、、、を繰り返す。-> min_depthを用意して更新する必要がある。

- BFSは幅優先なので、一段ずつ丁寧に深めて探索する。今回はBFSの方が直感的だと感じた。
- `if not root`のところはValueErrorを吐いても構わないと思う。(LeetCodeでは0で出して欲しいようだが。。。)

# Step 2
- 他の人のコードも見てみる。`is_leaf`のようなフラグを使っている人もいた。

```python
class Solution:
    def minDepth(self, root: Optional[TreeNode]) -> int:
        if not root:
            return 0

        nodes = [root]
        depth = 0
        while True:
            depth += 1
            next_depth_nodes = []
            for node in nodes:
                if node.left is None and node.right is None:
                    return depth
                if node.left:
                    next_depth_nodes.append(node.left)
                if node.right:
                    next_depth_nodes.append(node.right)
                nodes = next_depth_nodes
```

- Google Python Style Guideでは、Noneか否かの判定では`if foo is None`を使うべきとある。https://google.github.io/styleguide/pyguide.html#s2.14-truefalse-evaluations
- 例えばデフォルト値でNoneが設定されているシチュエーションで、0が渡された時と区別がつかないから。

## DFS
```python
class Solution:
    def minDepth(self, root: Optional[TreeNode]) -> int:
        if not root:
            return 0

        node_depth_pairs = [(root, 1)]  # This is a stack.
        min_depth = math.inf
        while node_depth_pairs:
            node, depth = node_depth_pairs.pop()
            if node.left is None and node.right is None:
                min_depth = min(min_depth, depth)
            if node.left:
                node_depth_pairs.append((node.left, depth + 1))
            if node.right:
                node_depth_pairs.append((node.right, depth + 1))
        
        return min_depth
```
- Step1では命名が下手だった。もっと丁寧に見ていきたい。

#  Step 3
```python
class Solution:
    def minDepth(self, root: Optional[TreeNode]) -> int:
        if not root:
            return 0
            
        current_level_nodes = [root]
        depth = 0
        
        while current_level_nodes:
            depth += 1
            next_level_nodes = []
            for node in current_level_nodes:
                if node.left is None and node.right is None:
                    return depth
                if node.left:
                    next_level_nodes.append(node.left)
                if node.right:
                    next_level_nodes.append(node.right)
            current_level_nodes = next_level_nodes
```
```python
class Solution:
    def minDepth(self, root: Optional[TreeNode]) -> int:
        if not root:
            return 0
        
        nodes_depth_pairs = [(root, 1)]
        min_depth = math.inf
        while nodes_depth_pairs:
            node, depth = nodes_depth_pairs.pop()
            if not node.left and not node.right:
                min_depth = min(min_depth, depth)
            if node.left:
                nodes_depth_pairs.append((node.left, depth + 1))
            if node.right:
                nodes_depth_pairs.append((node.right, depth + 1))
        
        return min_depth
```
- BFSとDFSをそれぞれ3回ずつ。ノードと深さをペアにする方法と、リストを２つ持ってきて深さを別で管理する方法を思いつく。
