# Step 1
## DFSで探索して、探索するごとに`island_size`をインクリメントしていって、`size_of_islands`に格納していき、その最大値を出力する。
### ↓Wrong Answer
```python
LAND = '1'
WATER = '0'

class Solution:
    def maxAreaOfIsland(self, grid: List[List[int]]) -> int:
        def is_in_grid(row, column):
            return 0 <= row < row_length and 0 <= column < column_length

        def visit_island(row, column, island_size) -> int:
            if not is_in_grid(row, column):
                return island_size
            if grid[row][column] == WATER or is_visited[row][column]:
                return island_size
            
            is_visited[row][column] = True
            island_size += 1
            visit_island(row + 1, column, island_size)
            visit_island(row - 1, column, island_size)
            visit_island(row, column + 1, island_size)
            visit_island(row, column - 1, island_size)

        row_length = len(grid)
        column_length = len(grid[0])
        is_visited = [[False] * column_length for _ in range(row_length)]
        size_of_islands = set()

        if not grid:
            return 0
        
        for row in range(row_length):
            for column in range(column_length):
                if grid[row][column] == LAND and not is_visited[row][column]:
                    size_of_islands.add(visit_island(row, column, 0))

        if not size_of_islands:
            return 0
        return max(size_of_islands)
```
- 200 Number of Islandsで探索したのだから、追加で大きさを格納するなんて簡単！と思ったが、思ったように動かない。

# Step 2
## 再帰DFSを用いる
```python
LAND = 1
WATER = 0
MAX_RECURSION = 10**4
sys.setrecursionlimit(MAX_RECURSION)

class Solution:
    def maxAreaOfIsland(self, grid: List[List[int]]) -> int:
        if not grid:
            return 0

        row_length = len(grid)
        column_length = len(grid[0])
        is_visited = [[False] * column_length for _ in range(row_length)]

        def is_in_grid(row, column):
            return 0 <= row < row_length and 0 <= column < column_length

        def traverse_and_measure_island(row, column):
            size = 0
            if not is_in_grid(row, column):
                return size
            if is_visited[row][column]:
                return size
            if grid[row][column] == WATER:
                return size
            is_visited[row][column] = True
            size += 1
            return (
                    size + traverse_and_measure_island(row + 1, column)
                    + traverse_and_measure_island(row - 1, column)
                    + traverse_and_measure_island(row, column + 1)
                    + traverse_and_measure_island(row, column - 1)
            )
            
        max_size = 0

        for row in range(row_length):
            for column in range(column_length):
                if grid[row][column] == LAND and not is_visited[row][column]:
                    max_size = max(max_size, traverse_and_measure_island(row, column))

        return max_size

```
- 再帰深さは最大50**2
- 時間計算量: O(nm) (Pythonだから実行時間は2500/100万second くらい？)
- 空間計算量: O(nm) (is_visitedとコールスタックはいずれもO(nm))

## 非再帰DFS
```python
LAND = 1
WATER = 0

class Solution:
    def maxAreaOfIsland(self, grid: List[List[int]]) -> int:
        if not grid:
            raise ValueError('invalid input')

        row_length = len(grid)
        column_length = len(grid[0])
        is_visited = [[False] * column_length for _ in range(row_length)]

        def is_in_grid(row, column):
            return 0 <= row < row_length and 0 <= column < column_length
        
        directions = [(1, 0), (-1, 0), (0, 1), (0, -1)]
        def walk_and_measure_island(row, column):
            size = 0
            lands_to_visit = [(row, column)]
            while lands_to_visit:
                present_row, present_column = lands_to_visit.pop()
                if not is_in_grid(present_row, present_column):
                    continue
                if grid[present_row][present_column] == WATER:
                    continue
                if is_visited[present_row][present_column]:
                    continue
                is_visited[present_row][present_column] = True
                size += 1
                for dir_row, dir_column in directions:
                    new_row = present_row + dir_row
                    new_column = present_column + dir_column
                    lands_to_visit.append((new_row, new_column))
            return size
                           
        max_size = 0
        for row in range(row_length):
            for column in range(column_length):
                if grid[row][column] == LAND and not is_visited[row][column]:
                    max_size = max(max_size, walk_and_measure_island(row, column))
        
        return max_size
```
- sizeじゃなくて普通に0と1でいいやんけと1人突っ込み。
## BFS
```python
LAND = 1
WATER = 0

class Solution:
    def maxAreaOfIsland(self, grid: List[List[int]]) -> int:
        if not grid:
            return 0
        
        row_length = len(grid)
        column_length = len(grid[0])
        is_visited = [[False] * column_length for _ in range(row_length)]
        
        def is_in_grid(row, column):
            return 0 <= row < row_length and 0 <= column < column_length
        
        directions = [(1, 0), (-1, 0), (0, 1), (0, -1)]
        def walk_and_measure_island(row, column):
            size = 0
            lands_to_visit = deque([(row, column)])
            while lands_to_visit:
                present_row, present_column = lands_to_visit.popleft()
                if not is_in_grid(present_row, present_column):
                    continue
                if grid[present_row][present_column] == WATER:
                    continue
                if is_visited[present_row][present_column]:
                    continue
                is_visited[present_row][present_column] = True
                size += 1
                for dir_row, dir_column in directions:
                    new_row = present_row + dir_row
                    new_column = present_column + dir_column
                    lands_to_visit.append((new_row, new_column))
            return size
            
        max_size = 0
        for row in range(row_length):
            for column in range(column_length):
                if grid[row][column] == LAND and not is_visited[row][column]:
                    max_size = max(max_size, walk_and_measure_island(row, column))
        
        return max_size
```
- https://docs.python.org/3/library/exceptions.html#TypeError
- `return size`を書き忘れてNoneが返ったからTypeErrorを吐かれた。TypeErrorはそもそも型があってない時に使う。ValueErrorは型は合ってるけど不適切な入力に対して。NotImplementedErrorは「現在は未実装だが、将来的に実装する可能性がある(??)」時に投げさせるらしい。


# Step 3
```python
LAND = 1
WATER = 0

class Solution:
    def maxAreaOfIsland(self, grid: List[List[int]]) -> int:
        if not grid:
            return 0

        row_length = len(grid)
        column_length = len(grid[0])
        is_visited = [[False] * column_length for _ in range(row_length)]
        
        def is_in_grid(row, column):
            return 0 <= row < row_length and 0 <= column < column_length
        
        def walk_and_measure_island(row, column):
            if not is_in_grid(row, column):
                return 0
            if is_visited[row][column]:
                return 0
            if grid[row][column] == WATER:
                return 0
            is_visited[row][column] = True
            return 1 + ( walk_and_measure_island(row - 1, column)
                         + walk_and_measure_island(row + 1, column)
                         + walk_and_measure_island(row, column - 1)
                         + walk_and_measure_island(row, column + 1)
                         )
        
        max_area = 0
        for row in range(row_length):
            for column in range(column_length):
                if grid[row][column] == LAND and not is_visited[row][column]:
                    max_area = max(max_area, walk_and_measure_island(row, column))
        
        return max_area

```
- max()は単純な線形探索で実装されている。
- 時間計算量: O(NM) (実行時間は25*10^-4s = 2.5msくらいか)
- 空間計算量: O(NM)
コールスタック深さは最大NM = 2500。
## 非再帰DFS
```python
LAND = 1
WATER = 0

class Solution:
    def maxAreaOfIsland(self, grid: List[List[int]]) -> int:
        if not grid:
            return 0
        row_length = len(grid)
        column_length = len(grid[0])
        is_visited = [[False] * column_length for _ in range(row_length)]
        
        def is_in_grid(row, column):
            return 0 <= row < row_length and 0 <= column < column_length
        
        def walk_and_measure_island(row, column):
            size = 0
            lands_to_visit = [(row, column)]
            while lands_to_visit:
                present_row,  present_column = lands_to_visit.pop()
                if not is_in_grid(present_row, present_column):
                    continue
                if is_visited[present_row][present_column]:
                    continue
                if grid[present_row][present_column] == WATER:
                    continue
                is_visited[present_row][present_column] = True
                size += 1
                directions = [(1, 0), (-1, 0), (0, 1), (0, -1)]
                for dir_row, dir_column in directions:
                    new_row = present_row + dir_row
                    new_column = present_column + dir_column
                    lands_to_visit.append((new_row, new_column))
            return size
                    
        
        max_area = 0
        for row in range(row_length):
            for column in range(column_length):
                if grid[row][column] == LAND and not is_visited[row][column]:
                    max_area = max(max_area, walk_and_measure_island(row, column))
        
        return max_area
```
- 時間計算量: O(NM)
- 空間計算量: O(NM)
## BFS
```python
LAND = 1
WATER = 0

class Solution:
    def maxAreaOfIsland(self, grid: List[List[int]]) -> int:
        if not grid:
            return 0

        row_length = len(grid)
        column_length = len(grid[0])
        is_visited = [[False] * column_length for _ in range(row_length)]
        
        def is_in_grid(row, column):
            return 0 <= row < row_length and 0 <= column < column_length
        
        def walk_and_measure_island(row, column):
            size = 0
            lands_to_visit = deque([(row, column)])
            while lands_to_visit:
                present_row, present_column = lands_to_visit.popleft()
                if not is_in_grid(present_row, present_column):
                    continue
                if grid[present_row][present_column] == WATER:
                    continue
                if is_visited[present_row][present_column]:
                    continue
                is_visited[present_row][present_column] = True
                size += 1
                directions = [(1, 0), (-1, 0), (0, 1), (0, -1)]
                for dir_row, dir_column in directions:
                    new_row = present_row + dir_row
                    new_column = present_column + dir_column
                    lands_to_visit.append((new_row, new_column))
            return size

        max_size = 0
        for row in range(row_length):
            for column in range(column_length):
                if grid[row][column] == LAND and not is_visited[row][column]:
                    max_size = max(max_size, walk_and_measure_island(row, column))
        
        return max_size
```
- 時間計算量: O(NM)
- 空間計算量: O(NM)
- dequeの実装を読みたいがC言語がうまく読めない。

LeetCodeのrun timeは当てにならないとは知っているが、それぞれ
- recursive DFS: 25ms 
- nonrecursive DFS: 39ms
- BFS: 37ms

だった。再帰DFSが一番早いのがわからなかった。nsオーダーとはいえ関数コールのオーバーヘッドが余計にかかるだろうに。cacheのヒット率が言及されていたが、よくわからなかった。
- BFSは近くに目的地がある時に使いやすく、DFSは遠くにある時に便利。
- https://www.geeksforgeeks.org/difference-between-bfs-and-dfs/

